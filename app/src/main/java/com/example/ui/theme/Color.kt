package com.example.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6750A4)
val PurpleGrey40 = Color(0xFF625B71)
val Pink40 = Color(0xFF7D5260)

// Geometric Balance themed colors
val SecurityDarkBg = Color(0xFFFEF7FF)     // App core canvas background (originally dark)
val SecurityCardBg = Color(0xFFE8DEF8)     // Primary card components container background (originally dark slate card)
val SecurityAccentBlue = Color(0xFF6750A4) // Dynamic modern primary action accent color (originally sky blue)

// Additional helper colors mapping standard dark surfaces/borders to elegant geometric light counterparts
val GeometricOverlayBg = Color(0xFFF3EDF7) // Inner field containers, standby overlay, unselected options background
val GeometricBorder = Color(0xFFEADDFF)    // Geometric component strokes & outer borders
val GeometricDivider = Color(0xFFE7E0EC)   // Fine section separators & alignment guidelines

// Theme text color tokens
val TextPrimaryDark = Color(0xFF1D1B20)    // High contrast typography for titles, major headers, values & cards
val TextSecondaryDark = Color(0xFF49454F)  // Media text, micro-copy, timestamps, supporting guidelines

val AuthenticGreen = Color(0xFF2E7D32)     // Richer green for light backgrounds to ensure AAA contrast
val SuspiciousAmber = Color(0xFFD84315)    // Deep bronze amber for visible caution labels on light layout
val CounterfeitRed = Color(0xFFC62828)     // High-accessibility crimson red for warning alerts
